What's new in this release:

- Bramble can now give every site its own email address. Connect the alias provider you already use, Addy.io or SimpleLogin, under Settings, Aliases, and a new button next to the username field creates a fresh forwarding address for that login. The site never learns your real address, and if one starts sending spam you can switch that alias off at your provider without touching anything else. Bramble only asks your provider for an address; your mail never passes through it.
- Or use a domain you already own, with no provider and no account at all. If your domain already delivers everything to your inbox, which a catch-all or forwarding rule at hosts like Migadu, Cloudflare Email Routing or Fastmail gives you, pick Your own domain under Settings, Aliases and Bramble makes each address on your device, instantly, without contacting anyone. It is the one option that works with no network. Bramble cannot tell whether the mail really reaches you, so send one of the addresses a test message before you rely on it.
- Your alias provider now travels with your vault. Set it up once and it reaches your other devices the next time they sync, so there is no pasting an API key into every phone and browser. It is stored inside your encrypted vault like everything else, which means it is only ever readable after you unlock.
- Nothing is created behind your back. An alias is only ever made when you ask for one, and until you connect a provider Bramble never contacts one at all.
- The password strength meter now estimates how many guesses a password would take, instead of counting capitals and symbols. It knows common passwords, keyboard runs, dates and the usual letter-for-number swaps, so a password like Password1! now reads as weak and a long passphrase reads as strong, which is the other way round from before. The same meter is used everywhere you choose a password: a new vault, your master password, a login entry and an export file.

No account, no cloud. Everything stays on your devices.
